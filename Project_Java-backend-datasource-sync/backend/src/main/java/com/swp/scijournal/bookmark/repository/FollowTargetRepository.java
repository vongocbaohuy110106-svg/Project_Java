package com.swp.scijournal.bookmark.repository;

import com.swp.scijournal.bookmark.entity.FollowTarget;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FollowTargetRepository extends JpaRepository<FollowTarget, Long> {
}
